<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishMapper">
    <resultMap id="dishVOResultMap" type="com.sky.vo.DishVO">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="categoryId" column="category_id"/>
        <result property="price" column="price"/>
        <result property="image" column="image"/>
        <result property="description" column="description"/>
        <result property="status" column="status"/>
        <result property="updateTime" column="update_time"/>
        <result property="categoryName" column="category_name"/>
        <collection property="flavors" ofType="com.sky.entity.DishFlavor">
            <id property="id" column="flavor_id"/>
            <result property="dishId" column="flavor_dish_id"/>
            <result property="name" column="flavor_name"/>
            <result property="value" column="flavor_value"/>
        </collection>
    </resultMap>
<!--    保存菜品和风味-->
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dish(name,
                         category_id,
                         price,
                         image,
                         description,
                         status,
                         create_time,
                         update_time,
                         create_user,
                         update_user)
        VALUES(#{name},
               #{categoryId},
               #{price},
               #{image},
               #{description},
               #{status},
               #{createTime},
               #{updateTime},
               #{createUser},
               #{updateUser});
    </insert>
    <!--通过id修改菜品-->
    <update id="updateDishWithFlavorsById" parameterType="com.sky.dto.DishDTO">
        UPDATE dish
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="image != null and image != ''">
                image = #{image},
            </if>
            <if test="description != null and description != ''">
                description = #{description},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        <where>
            <if test="id != null">
                id = #{id}
            </if>
        </where>
    </update>

    <update id="updateDishById">
    UPDATE dish
        <set>
            <if test=" name != null">
                name = #{name},
            </if>
            <if test="categoryId !=null">
                category_id = #{categoryId}
            </if>
        <if test="price != null">
            price = #{price},
        </if>
        <if test="image != null">
            image = #{image},
        </if>
        <if test="description != null">
            description = #{description},
        </if>
        <if test="status != null">
            status = #{status},
        </if>
        <if test="updateTime != null">
            update_time = #{updateTime},
        </if>
        <if test="updateUser != null">
            update_user = #{updateUser},
        </if>
        <if test="createTime != null">
            create_time = #{createTime},
        </if>
        <if test="createUser != null">
            create_user = #{createUser},
        </if>
         </set>
    <where>
        <if test="id != null">
            id = #{id}
        </if>
    </where>
    </update>
    <!--    根据id列表删除-->
    <delete id="deleteListById">
        DELETE FROM  dish WHERE id IN
        <foreach item="id" collection="list" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>
    <!--    通过id查询菜品-->
    <select id="selectDishById" resultType="com.sky.entity.Dish">
        SELECT * FROM dish
        <where>
            id = #{id}
        </where>
    </select>
<!--    通过菜品id查询菜品风味-->
    <select id="selectDishFlavorByDishId" resultType="com.sky.entity.DishFlavor"></select>
<!--    通过id查询菜品和口味-->
    <select id="getByIdWithFlavor" resultMap="dishVOResultMap">
        select d.id,
               d.name,
               d.category_id,
               d.price,
               d.image,
               d.description,
               d.status,
               d.update_time,
               c.name category_name,
               df.id flavor_id,
               df.dish_id flavor_dish_id,
               df.name flavor_name,
               df.value flavor_value
        from dish d
        left join dish_flavor df on d.id = df.dish_id
        left join category c on d.category_id = c.id
        where d.id = #{id}
    </select>
    <select id="getPage" resultType ="com.sky.entity.Dish">
        SELECT * FROM DISH
        <where>
            <if test ="name  != null">
                AND name LIKE concat('%',#{name},'%')
            </if>
        <if test="categoryId != null">
           AND category_id = #{categoryId}
        </if>
        <if test="status != null">
           AND status = #{status}
        </if>
        </where>
    </select>
<!--    通过分类id获取菜品和菜品口味-->
    <select id="getDishAndFlavorByCategoryId" resultType="com.sky.vo.DishVO">
        select d.id,
               d.name,
               d.category_id,
               d.price,
               d.image,
               d.description,
               d.status,
               d.update_time,
               df.id flavor_id,
               df.dish_id flavor_dish_id,
               df.name flavor_name,
               df.value flavor_value
        from dish d
        left join dish_flavor df on d.id = df.dish_id
        where d.category_id = #{categoryId}
    </select>
    <select id="getDishByName" resultType="com.sky.entity.Dish">
        SELECT * FROM dish
        <where>
            name = #{name}
        </where>
    </select>
    <select id="getDishById" resultType="com.sky.entity.Dish">
        SELECT * FROM DISH
        <where>
            id = #{id}
        </where>
    </select>
</mapper>
